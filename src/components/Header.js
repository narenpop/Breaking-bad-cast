import React from "react"
import logo from "../Img/logo.png"
import '../App.css'

function Header(){
    return(
      <header className="centre">
          <img src={logo} alt='' />
          </header>
    )
}
export default Header